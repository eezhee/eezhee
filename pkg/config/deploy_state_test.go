package config

import (
	"testing"
)

func setup() {
	// make sure there isn't a deploy-state.yaml file
	// if there is stash it
}

func cleanup() {
	// if we stashed a state file, restore it
}

// TestNoStateFile tests if there is no state file
func TestNoStateFile(t *testing.T) {

	// make sure there is no state file

}
